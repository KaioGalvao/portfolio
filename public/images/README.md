# Images folder

Place your project images here. For the Flood Guard featured project, save the provided screenshot as:

- floodguard-hero.png

Then it will automatically be used by the Projects section via `/images/floodguard-hero.png`.
